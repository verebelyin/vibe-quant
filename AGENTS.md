# AGENTS.md

All project instructions live in [CLAUDE.md](CLAUDE.md) — read that file in full before working in this repo.

(This pointer exists for non-Claude agent tools that look for AGENTS.md. It was previously a byte-identical copy of CLAUDE.md, which made Claude Code load the same ~19KB twice per session.)
